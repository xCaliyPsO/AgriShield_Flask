# Flask ML API Requirements - Optimized for Heroku (CPU-only PyTorch)
# Core Flask
Flask==2.3.0
flask-cors==4.0.0
gunicorn==21.2.0

# PyTorch CPU-only (saves ~2GB vs CUDA version)
# Install from PyPI CPU-only wheels
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.0.1+cpu
torchvision==0.15.2+cpu

# Ultralytics (YOLO) - latest stable
ultralytics==8.0.196

# Essential dependencies (minimal versions)
numpy==1.24.3
Pillow==10.0.0
pymysql==1.1.0
python-dotenv==1.0.0
requests==2.31.0

# Removed to save space (not needed for inference):
# - scikit-learn (~50MB)
# - matplotlib (~30MB) 
# - seaborn (~20MB)
